import { FilterAmountsPipe } from './filter-amounts.pipe';

describe('FilterAmountsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAmountsPipe();
    expect(pipe).toBeTruthy();
  });
});
